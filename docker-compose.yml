version: "3.9"

services:
  db:
    image: postgres
    environment: 
      - POSTGRES_DB=food_auto_placer_db
      - POSTGRES_USER=myhailo_admin
      - POSTGRES_PASSWORD=Kdou3SM829LFms9Kks8D
  web:
    build: .
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/code
    ports:
      - "8000:8000"
    depends_on:
      - db